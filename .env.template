# ---------------------------------------------------
# General Organization Details
# ---------------------------------------------------
# Used as the default in the vCard form and for the pass organization name.
VITE_ORG_NAME="Organization Name"
# Used as the default website in the vCard form.
VITE_ORG_WEBSITE="https://www.example.com"
# Used as the office phone number options in the vCard form.
VITE_OFFICE_PHONE_OPTIONS='[{"key":"example.com","phone_options":[{"display":"(123) 456-7890 – Location Name","value":"+1234567890"},{"display":"(222) 333-4444 – Another Location","value":"+12223334444"}]}]'

# ---------------------------------------------------
# Frontend Logo Configuration (Production)
# ---------------------------------------------------
# In production, this entire JSON object should be provided as a single-line string
# in the VITE_LOGOS_CONFIG GitHub secret.
# In development, the app will read from `frontend/src/config/logos.json` directly.
VITE_LOGOS_CONFIG='{"default":{"main":["Logo.svg"],"wifi":["WiFi_icon.svg"]},"example.com":{"main":["Example.com-Logo-1.svg","Example.com-Logo-2.svg"],"wifi":["Example.com-WiFi.svg"]},"example.org":{"main":["Example.org-Logo.svg"]}}'

# ---------------------------------------------------
# Development Settings
# ---------------------------------------------------
# Set to 'true' to enable visual debugging overlays and console logs for the dot removal algorithm.
VITE_VISUAL_DEBUG=false

# ---------------------------------------------------
# Frontend Preset Configuration
# ---------------------------------------------------
# In production, this entire JSON object should be provided as a single-line string
# in the VITE_PRESETS_CONFIG GitHub secret.
VITE_PRESETS_CONFIG='{"example":{"org":"Example Organization","website":"example.org","logoUrl":"/example.svg","dotsColor":"#006600","cornersSquareColor":"#009900","cornersDotColor":"#006600"}}'


# ---------------------------------------------------
# Apple Wallet PassKit Configuration (Production)
# ---------------------------------------------------
# In production, this entire JSON object should be provided as a single-line string.
# In development, the app will read from `server/src/config/pass-templates.json` instead.
PASS_CONFIG='{"default":{"model":"default.pass",...},"templates":[{"domain":"example.org",...}]}'


# ---------------------------------------------------
# Certificate Filenames (for local development)
# ---------------------------------------------------
# These should match the files in /server/certs
CERT_SIGNER_KEY="signerKey.key"
CERT_SIGNER_CERT="signerCert.pem"
CERT_WWDR="AppleWWDRCAG4.pem"

# ---------------------------------------------------
# Domain & URL Configuration
# ---------------------------------------------------
# The production domain for the frontend
FRONTEND_DOMAIN="qr.example.com"
# The production domain for the backend API
BACKEND_DOMAIN="pkpass.example.com"
# A JSON object mapping email domains to their respective photo lookup service URLs.
PHOTO_SERVICE_URL='{"example.com": "https://api.example.com"}'
# The GitHub repository in the format 'owner/repo'
GITHUB_REPO="Standearth/vcard-qr"

# ---------------------------------------------------
# Google Wallet Configuration (Local Development)
# ---------------------------------------------------
# Your Google Wallet Issuer ID
GOOGLE_ISSUER_ID="3388000000012345678"
# For local development, the app uses a service account JSON key file.
# By default, it looks for 'server/certs/google-wallet-sa-key.json'.
# You can override this path by setting the following environment variable:
# GOOGLE_WALLET_KEY_PATH="path/to/your/key.json"
